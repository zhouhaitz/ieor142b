Legacy **train / validation / test** splits for the **original full** `MovieGenre.csv` workflow (`main.py`).

- Generated by `scripts/generate_splits.py` with `random_state=42`, matching `main.py`.
- Files: `train_rows.csv`, `val_rows.csv`, `test_rows.csv` with column **`row_id`** = row index into the cleaned dataframe (`dropna` on `Genre`, then `reset_index(drop=True)`), same order as `generate_splits.py`.

Using row ids avoids ambiguity if `imdbId` appears on multiple CSV rows.

Do not hand-edit unless the team agrees to re-run the generator after changing `MovieGenre.csv`.
